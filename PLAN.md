# 字帖网站开发计划（v2 — 含硬笔/钢笔/毛笔三大字帖体系）

## 一、产品定位

浏览器里的中文练字帖生成器：**输入文字 → 选字帖类型 → 调样式 → 直接打印/导出**。
无需安装，即用即印。目标用户：小学生家长、语文老师、成人练字者、书法爱好者。

核心原则：**打印质量是底线**。字帖最终 consumed 在纸上，一切以 A4 打印保真为准绳。

## 二、字帖类型体系（顶层维度）

用户在第一步就选择字帖类型，后续格子、字体、版式、配色都随类型切换默认值。

| 维度 | 铅笔帖（幼小衔接/一年级） | 钢笔帖（二年级+/成人） | 毛笔帖（软笔书法） |
|---|---|---|---|
| 典型格子 | 田字格、拼音田字格，12–18mm | 田字格/方格/米字格，8–12mm；也可"行款线"（无格横线） | 米字格、九宫格、回米格、十字大格，40–70mm |
| 书体/字体 | 规范楷书、手写体 | 硬笔楷书、行楷 | 毛笔楷书（颜柳欧赵风格）、行、草、隶、篆 |
| 颜色习惯 | 浅灰/淡红描红 | 灰、蓝黑描红 | 朱红描红、双钩、碑帖黑底白字 |
| 典型版式 | 笔顺行 + 拼音行 + 描红行 + 空白行 | 段落行款、古诗词通篇、生字条 | 大字单行描红、双钩填廓、碑版样式 |
| 纸张习惯 | A4 | A4 | A4（大字每页 4×4 或 3×4 格）|

> 预留扩展：英语四线三格帖、数字/字母帖（P2 期）。

## 三、格子体系（参数化设计）

格子引擎不硬编码"一种格子一段代码"，而是**线组组合**：任意格子 = 方形外框 + 内部线组（十字 / 对角线 / 九宫 3×3 / 内框"回" / 定位点 / 比例定位线）的任意搭配，样式（虚实、粗细、颜色、底色）是独立维度。新增格子基本等于"加一个预设"，后期甚至可以开放用户自定义线组。

**A. 结构格（汉字间架，M1 全覆盖）**
- 田字格、米字格、九宫格、回宫格、回米格
- 方格（空格）、十字虚线格、对角线格（仅 X 线）
- 定位点格（中心点 + 四角点）、虚格（仅四角定位点，进阶脱格训练用）
- 357 定位格（市面流行硬笔定位法，实施时按实物格子样张校准线位）

**B. 拼音 / 外语格（M2）**
- 拼音三线格、拼音田字格、拼音方格
- 英语四线三格、英语书写斜线格

**C. 多字 / 篇章格（M3）**
- 作文格（400/500 字作文纸样式）、百字格
- 竖排方格（传统竖写，从右至左）
- 五言/七言诗创作纸

**D. 书法创作版式（版式维度，M3~M4）**
竖排 + 装饰边框，本质是版式模板而非格子：条幅、斗方、横披、中堂、**对联**（上下联成对排版）、扇面（团扇/折扇——扇形弯曲格线，正是 SVG 的用武之地，放 M4）。

**E. 格子样式选项（所有格子通用，M1 起即有）**
实线/虚线、线宽/线色、单/双线外框、格心点开关、格子底色（含米黄宣纸底）。

## 四、功能清单（按优先级）

### P0 — MVP
- 字帖类型选择（铅笔/钢笔/毛笔），切换时自动带出该类型的默认格子、字体、尺寸、配色
- 文字输入：自由输入/粘贴，实时预览
- 格子类型：第三节 A 组结构格全覆盖（田/米/九宫/回宫/回米/方/十字虚线/对角线/定位点/虚格/357 定位格）+ 横线行款格；M2 起补 B 组拼音/英语格
- 字体：内置硬笔 2~3 款 + 毛笔 1~2 款（全部免费商用）
- 字号 = 格子边长（mm 精确控制），每页行/列数
- 颜色：文字颜色/不透明度、格子线颜色/粗细、格子底色
- 描红模式：范字淡色打底 + 空白格交替（比例可调，如 1 描 2 空）
- A4 打印：页边距、横/竖向、缩放 100% 提示

### P1 — 字帖核心体验
- 拼音：字上方注音，多音字手动改读音，拼音/注音符号两种格式
- 笔画展示：静态笔顺编号、笔画分解着色；屏幕预览笔顺动画（hanzi-writer）
- 硬笔"生字行"版式模板：`生字+拼音 → 笔顺 → 描红×2 → 空白×3` 自动循环
- 毛笔专用：双钩模式（只描轮廓）、碑帖模式（黑底白字）、大字版式预设（每页 16/12 格）
- 页眉页脚：姓名/日期/班级填写位、页码
- 虚线回宫格、格心标记开关

### P2 — 增强
- 内容模板库：古诗词、成语、部编版识字表（1-2 年级 300 字等）、千字文/三字经（毛笔帖刚需）
- 导出 PDF（矢量）/ PNG；配置保存 localStorage；URL 编码配置实现分享链接
- 字体库扩充（详见字体表）、笔法/结构侧边标注（毛笔帖）
- 移动端适配、英语四线三格

## 五、字体方案（重点，版权敏感）

**原则：只用 OFL / 明确免费商用字体；商用碑帖风格字体（方正颜宋、田英章、荆霄鹏等）一律不用，除非用户自行取得授权。**

### 硬笔 / 通用
| 字体 | 风格 | 许可 | 获取 |
|---|---|---|---|
| 霞鹜文楷 LXGW WenKai | 楷体（硬笔质感） | OFL | npm 包 `lxgw-wenkai-webfont` / jsDelivr |
| 杨任东竹石体 | 手写楷书 | OFL | Google Fonts |
| 站酷小薇 / 快乐体 / 庆科黄油体 | 手写/创意 | OFL | Google Fonts（已切片，按需加载） |
| 李国夫手写体 | 硬笔手写 | 免费商用 | 官网下载，自托管 woff2 |

### 毛笔
| 字体 | 书体 | 许可 | 获取 |
|---|---|---|---|
| 马善政毛笔楷书 Ma Shan Zheng | 楷书 | OFL | Google Fonts |
| 龙藏体 Long Cang | 行书 | OFL | Google Fonts |
| 织芒行 Zhi Mang Xing | 行草 | OFL | Google Fonts |
| 柳建毛草 Liu Jian Mao Cao | 草书 | OFL | Google Fonts |
| 方舟楷体 / 方舟仿宋 | 楷书（近欧/赵） | 免费商用 | 方舟字库官网，自托管 |
| 隶书、篆书、颜柳欧赵精准碑帖风格 | — | **开源圈基本空白** | 风险项，见第六节；MVP 用马善政/方舟楷替代 |

### 字体工程策略
- 优先 Google Fonts / CDN 托管：woff2 按 unicode-range 自动切片，只下载用到的字，解决 CJK 字体 5~20MB 的体积问题
- 自托管字体本地放 `public/fonts/`，构建时用 fontTools 子集化（覆盖常用 3500 字 + 模板内容用字）
- 缺字检测：渲染前用 FontFace.check() 探测，缺字自动降级为默认楷体并提示

## 六、技术方案

**纯前端静态站，无后端**（MVP 阶段）。数据 localStorage，分享靠 URL。部署 GitHub Pages / Vercel。

| 模块 | 选型 | 说明 |
|---|---|---|
| 框架 | Vite + React + TypeScript | 配置面板 + 预览的典型组件化结构 |
| 样式 | Tailwind CSS | 快速搭建设置面板 |
| 渲染核心 | **SVG** | 矢量打印不糊；格子=线条，范字=`<text>`，双钩=`<text>` stroke-only |
| 文字渲染 | 一期 SVG `<text>` + webfont；二期 opentype.js 转路径 | 转路径后打印效果跨环境一致 |
| 笔顺 | hanzi-writer + hanzi-writer-data（Makemeahanzi，约 8500 字） | 动画 + 笔画路径提取 |
| 拼音 | pinyin-pro（MIT） | 多音字、注音符号 |
| 页面规格 | 常量模块：A4 210×297mm，安全区、页边距、行列自动排布算法 | 预览即所见，用 CSS `@page` + mm 单位 |
| 打印 | `@media print` 隐藏面板，只输出页面 | PDF 导出三期 svg2pdf.js |
| 状态 | Zustand（轻） | 配置对象单一数据源，驱动预览与打印 |

**页面结构**：单页工作台。
- 顶部：字帖类型 Tab（铅笔帖｜钢笔帖｜毛笔帖｜扩展）、打印、导出、重置
- 左侧设置面板（分组折叠）：内容 / 格子 / 字体 / 描红与笔画 / 拼音 / 版式与颜色
- 右侧：A4 分页预览（翻页、缩放、页码指示）
- 移动端：面板收为底部抽屉

**项目结构（草案）**
```
src/
  components/
    SettingsPanel/   # 各组设置控件
    Preview/         # A4 分页预览、打印容器
    GridRenderer/    # 参数化格子引擎：外框+线组组合（十字/对角/九宫/回宫/定位点/比例线）
  core/
    layout.ts        # 行列排布、分页算法（mm 单位）
    fonts.ts         # 字体注册、加载、缺字检测
    strokes.ts       # hanzi-writer 封装
    pinyin.ts        # pinyin-pro 封装、多音字覆盖表
  data/
    presets.ts       # 三大字帖类型的默认配置预设
    templates.ts     # 古诗词/识字表内容库
```

## 七、里程碑

- **M1 — 硬笔帖 MVP**：脚手架；参数化 SVG 格子引擎（A 组结构格 + E 组样式选项全量）；字体/字号/颜色；描红模式；三大类型 Tab 与预设切换；A4 打印可用。
- **M2 — 注音与版式**：拼音（多音字交互）；笔顺编号 + 动画预览；硬笔生字行模板；B 组拼音格/英语四线三格；分页与页眉完善。
- **M3 — 毛笔帖专题**：大格米字/九宫/回米；毛笔字体上线（马善政、方舟楷等）；双钩与碑帖黑底模式；竖排书写方向；条幅/斗方/对联版式；作文格/诗笺；大字版式预设；钢笔行款/通篇版式。
- **M4 — 内容生态与导出**：内容模板库；PDF/PNG 导出；URL 分享；localStorage 存档；扇面（团扇/折扇）弯曲格线；移动端适配。

每个里程碑结束都交付一个"可打印"的可用版本。

## 八、风险与对策

1. **毛笔字体版权**：隶书/篆书/颜柳欧赵精准风格字体开源圈空白。对策：MVP 用 OFL 楷书系；后期两条路——采购商业授权（方正字库按 PV 计价），或与独立书家合作收录字体。**绝不上来路不明的字体。**
2. **打印保真**：浏览器默认"缩放至可打印区域"会破坏 mm 排版。对策：mm 精确布局 + 首次打印弹窗提示勾选 100%/实际大小 + 打印测试页（1cm 方格自校验）。
3. **碑帖模式费墨**：黑底大面积印张。对策：提供"浅灰底"替代版本并在 UI 提示。
4. **笔画数据覆盖**：Makemeahanzi 约 8500 字，生僻字缺数据 → 自动降级只印整字。
5. **多音字**：自动注音必错（银行/行走）→ 必须配点击改音交互，并记住用户修正。
6. **CJK 字体体积**：靠 CDN unicode-range 切片 + 子集化控制首屏，目标字体相关流量 < 2MB。

## 九、已确认决策（2026-09-18）

1. ✅ 纯前端、无后端（未来要账号/云存档再演进）。
2. ✅ 毛笔帖置于 M3，先跑通硬笔打印链路。
3. ✅ 无商业授权预算，**站点自用**：字体策略调整为——本地系统字体 + 自定义字体名输入为主（自用无再分发问题，隶/篆/碑帖风字体可自行安装到系统后直接调用），内置 OFL 开源字体（霞鹜文楷）兜底。若未来公开上线，再回到第五节的版权约束。

> 状态：**M4 已交付**（2026-09-18，M1–M4 同日交付，**计划内功能全部完成**）。
> - M1：三大字帖类型预设切换、12 种格子 + 样式选项（线色/线粗/双线框/格心点/底色）、描红模式（描/空数量可调）、字体（内置霞鹜文楷 + 系统字体 + 自定义字体名）、字号（格子边长 mm）、颜色/浓度、A4 mm 级排版与分页、页眉（标题+姓名日期栏）、页码、打印 CSS（`@page A4` + `@media print`）。
> - M2 拼音（pinyin-pro，MIT）：格上方四线三格拼音带 + 逐字注音（词组上下文自动选音）；多音字候选手动改读（覆盖表）；声调开关（可去调）；拼音大小/空白格是否注音。
> - M2 笔顺（hanzi-writer-data 本地数据，Makemeahanzi）：纸面「笔画累进分步格」（灰=已写、红=当前笔、右上角白底圆徽编号），可设仅首格编号或关闭；屏幕端 hanzi-writer 循环笔顺动画；数据经 Vite 中间件/`dist/hanzi-data` 本地提供（9500+ 字，离线可用，CDN 兜底），无数据字自动降级为整字。
> - M3 竖排：横排/竖排切换（自上而下、自右向左，列优先分页）；竖排不支持拼音带与笔顺（UI 提示与渲染规则一致）。
> - M3 毛笔版式：竖幅条幅（超长自动分页）、斗方、对联（上联居右/下联居左，按行或折半拆分），竖写字号随内容自适应、淡格线、深色碑帖配色。
> - M3 双钩：范字可选空心（描边轮廓），适合毛笔临帖。
> - M3 硬笔生字行模板：一键「拼音＋笔顺＋田字格＋描红/空白」（数量跟随面板当前设置，按钮文案动态显示，改数量再套模板不会被拍回固定值）；英语四线格：四线三格（第二条虚线）、按单词分格、长词自动缩字号。
> - 实测通过：字形 em 框与格子中心误差 0mm；分页/页眉/页码正确；拼音带四线与拼音落位、56 笔=56 个角标、多音字「石→dàn」实时联动、笔顺动画渲染、拼音/笔顺/横线格组合回归通过；竖排 9/9/7 分页、对联 14 字双框、斗方 2×2、双钩 fill=none、英语字号自适应（eye 7.26mm / pineapple 2.66mm）；视觉验收（编号可读性、拼音不压线、竖排碑帖/对联/英语格）经截图审查通过。
> - M4 内容库（`src/core/content.ts`）：公版内容 4 分类 35 篇——启蒙古诗词 18 首（咏鹅/静夜思/春晓…）、常用字表 6 组（启蒙基本字/高频字前96/一年级生字/数字方位/自然气象/人体动作）、经典蒙学 6 篇（三字经/千字文/百家姓/弟子规/笠翁对韵/声律启蒙）、励志短句与对联 5 组。面板「内容库」分类+篇目两级选择，选择即替换练习文字，亦可一键追加到现有文字之后。
> - M4 配置持久化（`src/core/persist.ts`）：配置变更实时写 localStorage（`copybook.config.v1`），启动时恢复并与默认值浅合并；旧档缺字段自动补默认，失效枚举/越界字号回退，隐私模式与 JSON 损坏静默降级。重置按钮回默认并同步覆盖存档。
> - M4 导出图片（`src/core/exportPng.ts`）：预览区每页 SVG → `<img>` → canvas → 300dpi PNG（A4 2480×3508）逐页下载。关键处理：`<img>` 渲染 SVG 无法引用页面 Web 字体，导出前扫描 `document.styleSheets`，仅把 unicode-range 覆盖本页文字的霞鹜文楷 woff2 分片 fetch 成 base64 内联进 SVG `<style>`（本次实测 48 片/2.2MB）；系统安装字体（楷体等）无需内联。工具栏显示导出结果统计。
> - M4 实测通过：内容库选篇 → 文本/存档/渲染三处联动；刷新后文本与格子类型自 localStorage 恢复；重置回默认且同步存档；导出 1 页与 4 页均生成正确命名的 PNG（drawImage 2480×3508）；导出 SVG 内嵌 48 个 `@font-face` data 字体，iframe 渲染截图经视觉验收确认楷书笔画特征保留（非宋体回退）、拼音/田字格/笔顺编号完整、无豆腐块。
> - M5 增补（2026-09-18）：① 我的模板（`src/core/templates.ts`）——整套配置（含练习文字）命名保存到 localStorage（`copybook.templates.v1`，与当前配置自动存档分开），列表展示摘要（格子/字号/描红空白/字数），一键套用、删除，同名保存即更新；② 听写帖一键预设「全空白格＋拼音提示」（描红 0＋空白×8＋空白格注音），配套改造：描红数量允许 0、格子引入分组号 `g`、空白格拼音只标每组第一格（听写卷形态）；③ 「英语四线格描红」一键预设（四线格＋描红×1＋空白×2）。实测：听写帖 16 组=16 条拼音且视觉确认只在组首；模板保存→刷新持久→套用还原→删除全链路通过；模板按钮（跟随数量）回归正常。构建通过。
> - 五轮 GUI 黑盒测试（2026-09-18，主 agent 按 web-gui-tester 方法论执行）：24 测试点，覆盖格子/字帖类型/版式（横竖排、对联、条幅、斗方）/拼音（去调、多音字）/笔顺（三模式计数）/内容库/模板 CRUD/持久化/PNG 导出/边界（空文本、越界钳制、超长词、未知字体回退）。发现并当日修复：**① 横线行款格误渲染笔顺分步格**（App.tsx `strokeEnabled` 补 `!ruled` 排除，与面板提示一致）；**② 碑帖深色配色下拼音/页眉/页码低对比**（GridSheet 增加 `luma()` 深浅判断，深底拼音→#e6d9b8、页眉→#d8cdb6、页码→#b9b2a5；白底笔顺编号徽按范字亮度选深字；已写笔画深底改暗灰与当前笔拉开层次）；**③ P3 三项**：空白文本显示空态提示（原渲染空白页）、英语单词最小字号 2mm（避免缩到 1mm 不可读）、自定义字体名 placeholder 截断（缩短为「本地字体名」+ title 提示）。修复后 DOM+视觉双重复验：横线格 0 徽标、碑帖拼音可读、空态出现/恢复、20 字母词 fs=2、默认态回归（拼音 #333、徽标 #e8736a）全部通过。证据：`gui-test-screenshots/`（基线、五轮关键状态、缺陷前后对比 11 张）。
> - M6 增补（2026-09-18，两批次交付 1–6 项增强）：① 选页导出——工具栏新增页码输入框（`parsePageSpec` 支持「1-3,5」「2~4」及中文顿号混排，空=全部），单页直接下载 PNG、多页 JSZip 打 zip（不再连弹下载），越界/脏页码即时报错；顺带修复连续导出时旧 8s 定时器提前清空新提示消息的竞态（timer 存 `useRef`）。② 毛笔版式选「竖幅条幅/斗方/对联」自动 `landscape:false` 切竖版纸（自由格阵列不动用户横竖选择）。③ 深底（碑帖）拼音加大 12%＋加粗 600＋不透明（浅底维持 #333/0.9）。④ 内容库新增「拼音练习」专帖（单韵母/声母/复韵母/前后鼻韵母/整体认读/四声共 6 篇）。⑤ 新增「英语词库」（字母大小写、元音 20/辅音 28 音标、文具/动物/颜色/数字/水果/家庭/身体/天气季节 8 组主题词）；两分类选篇自动切换英语四线格（`ContentCategory.grid` 驱动），选回中文篇目自动切回田字格。⑥ 古诗词按「一/二/三年级」分组，补充统编版必背公版篇目 15 首（赠汪伦、寻隐者不遇、画鸡、江雪、夜宿山寺、晓出净慈寺送林子方、山行、赠刘景文、夜书所见、望天门山、饮湖上初晴后雨、望洞庭、早发白帝城、忆江南、元日）。实测：8 页文档选「1」单发、「1-2,4」zip（消息「已导出 3 页 PNG 并打包 zip」）、「99」「2-」报错准确；条幅 297mm→210mm 自动竖版；碑帖拼音 #333/3.08/0.9→#e6d9b8/3.45/1.0/600 全量转换且可还原；音标页（含 ɪ ə ʃ ː 等 IPA）与碑帖拼音页视觉验收无豆腐块、对比度合格。tsc 与构建通过。
> - 默认值调整（2026-09-18）：笔顺不再默认显示——`DEFAULT_CONFIG.strokeMode` 与铅笔帖预设由 `'first'` 改 `'off'`，打开即是「整字范字＋拼音」的常规描红帖；笔顺仍可通过「笔顺 → 笔顺分步格」下拉（每字首次/每次出现）或「生字行模板」按钮按需开启。旧存档含 `strokeMode:'first'` 的用户点一次「重置」即切到新默认。
> - 全面代码审查与修复（2026-09-18）：逐文件审查后修复 4 处——① 内容库「四声练习」u 行笔误 `'ú ú ǔ ù'` → `'ū ú ǔ ù'`；② 毛笔版式（CalligraphySheet）页眉/页码在碑帖深底低对比：`luma()`/`isDarkBg()` 提为共享 `src/core/color.ts`（GridSheet 同步改用），深底页眉→#d8cdb6、页码→#b9b2a5，与 GridSheet 行为对齐；③ 「每行格数」手填值此前不校验页宽（colsOverride=30 时格子居中偏出纸面），`computeGeometry` 钳制为不超过页宽可容列数（只许改小不许加宽）；④ 对联/斗方超长约 70 字时被 4mm 字号下限顶出页底：上下联按页高同步切段分页（每段一页，页码自动连续），斗方按页内容量分页并在超高时回推列数。回归：tsc/构建通过；数值脚本 13/13（深底判断、钳制、超长联 160 字 2 页不越界、5000 字斗方 2 页不越界、常规单页与条幅分页行为不变）；浏览器复验碑帖对联页眉/页码换亮墨色且恢复「默认」配色后回 #555、每行格数 30 时内容盒 [14,196] 在 210mm 页宽内、重置回默认正常。遗留 P3（自用场景不修）：persist 仅校验枚举与字号（数字字段不逐项查类型）、导出 20+ 页内存占用、无 lint/单测。
> - 二轮审查修复（2026-09-18）：再全量审源文件后修 N1–N4——① 对联字号补宽度上限 `(页宽−双边框外扩)/2.9`（此前「边长」滑到 80–90mm 配短联会横向出纸，实测修复后 90mm 短联框位 [14.1,195.9] 在边距内、默认 60mm 不受影响）；② 对联分页时先耗尽一侧不再渲染 0 字空框；③ 「每行格数」输入即时钳到 1–30，显示值与渲染口径一致；④ dev 笔画数据中间件拒绝空字节/非法百分号编码路径（curl 复验 %00 不再崩，残余 %zz 500 为 Vite 内部 URL 解码既有行为，插件层无法代管）。N5「听写帖跨页组不标拼音」经数值复现为**误报**——页内首格无前驱、拼音本就会重新标注，未改代码。回归：tsc/构建/数值脚本全过，浏览器实测 N1/N3 后已重置回默认。仍接受的 P3：persist 数字字段不逐项校验、笔顺加载占位、导出全内存、无 lint/单测。
> - 六项可选增强全部交付（2026-09-18，「都做，一项项来」）：① **分享链接**——工具栏「分享链接」把整套配置（含练习文字）经 `sanitize` 后 JSON→UTF-8→base64url 编入 `#c=`（`src/core/share.ts`），clipboard 写入失败自动退回改写 hash；hash 只是启动种子，优先于 localStorage 存档、首次改动即 `replaceState` 清除。② **本地字体导入**——字体组新增文件选择（ttf/otf/woff/woff2），`FontFace` 注册为 `ImportedCopybookFont` 即时用于预览；因 `<img>` 光栅化读不到 `document.fonts`，PNG 导出路径把同一份 base64 以 `@font-face` 内联进导出 CSS（`src/core/fonts.ts`，会话级不持久，刷新后需重新导入，界面已提示）。③ **PWA 离线**——`vite-plugin-pwa`（autoUpdate）：预缓存只收 `index.html/registerSW.js/assets/**`（**绝不**打包 hanzi-data 的 9580 个 JSON），笔画数据与 woff2 子集走 RuntimeCache CacheFirst（≤180 天），manifest 用新 `public/icon.svg`；首访后完全离线可用。④ **页码历史**——选页导出成功后记入 `copybook.pagespec.v1`（去重、最多 5 条），下次打开自动回填最近写法并提供 datalist 候选。⑤ **缩略图导航**——页面 ≥3 时右侧固定竖排缩略条（`src/components/PageNav.tsx`），克隆各页 SVG 按 viewBox 缩到 8.5%，点击平滑滚到该页、滚动高亮当前页；克隆体去掉 `svg.sheet` 类名不污染导出查询，按钮 `flex-shrink:0` 防多页压扁。⑥ **打印收起面板**——打印 CSS 早已有 `display:none` 规则，本轮补上 `.page-nav`。实测（dev + 构建后浏览器 E2E）：分享 URL 打开后配置全量还原且优先于被篡改的本地存档、一编辑 hash 即清；导入 59KB woff2 后 FontFace=loaded、画布字体族切换、导出含内嵌字体成功（提示语完整）；「7-8」导出成功后刷新回填+候选、越界页码报错准确；31 页文档导航 31 缩略图、`svg.sheet` 计数不被克隆污染、点击/高亮跟随正确（后台标签页 rAF 节流只影响 smooth 动画演示）；测试残留已清、复位默认。tsc 与构建通过，预缓存清单 6 项（约 1.1MB）。
> - 三项后续增强（2026-09-18）：① **导入字体持久化**——字体数据（ArrayBuffer 原样，≤20MB 上限）结构化克隆存入 IndexedDB（`copybook.font` 库单仓单键），启动 `restoreImportedFont()` 自动重建 FontFace；`subscribeImported` 订阅 + `useSyncExternalStore` 让「当前已导入」提示跨异步恢复实时刷新，并提供「移除」按钮（连持久副本一起删，若正选用则回退默认字体）。隐私模式/配额不足时静默退化为仅本次会话。② **分享链接压缩**——deflate-raw（浏览器原生 `CompressionStream`，零依赖）+ `z` 前缀标记，解码端新旧格式通吃、旧链接永不失效（JSON 以 `{` 开头 → 旧格式 base64url 首字符恒为 `e`，与 `z` 无歧义）；`encodeShare/decodeShare/shareUrl` 转异步，App 启动改为「先渲染本地存档，解码完成后覆盖；期间用户已编辑则丢弃链接配置」。实测 160 字练习：URL 1608→631 字符（省 61%），压缩链/旧格式链均还原成功、坏链静默回退。③ **导出文件名**——`标题_YYYY-MM-DD_4页.zip`、zip 内 `标题_日期_第N页.png`（标题取页眉标题，滤文件系统非法字符、40 字截断、空退回「字帖」），拿给打印店不再一脸茫然。回归：tsc/构建通过；浏览器实测导入 59KB woff2→刷新自动恢复（FontFace=loaded、提示同步）→移除后刷新不复活；分享按钮剪贴板被拒时回退写入地址栏（630 字符压缩 hash）亦验证。测试残留与 IndexedDB 已清、复位默认。
> - 部署就绪（2026-09-18）：`base:'./'` 让产物零绝对路径（strokes.ts 本就是相对 hanzi-data 路径、manifest `start_url:'.'`），实测把 dist 挂到 `/copybook/` 子路径下页面渲染/文楷字体/Service Worker/manifest/笔画 JSON 全部正常——根域、GitHub Pages 项目页、宝塔子目录皆可直部署。附 `.gitignore` 与 `.github/workflows/deploy.yml`（push main → 构建 → Actions 发 Pages，绕开 Jekyll 且无需把 79MB 产物提交进 git）。dist 体积 79MB（hanzi-data 49MB + 文楷分片 ~29MB），Pages/宝塔均可承受；Service Worker 需 HTTPS（GH Pages 自带；宝塔签一张 Let's Encrypt 即可，未上 HTTPS 时网站照常、仅离线缓存不启用）。
